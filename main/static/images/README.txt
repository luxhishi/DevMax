Place your website tab icon PNG at this exact path:

- `main/static/images/site-icon.png`

The shared layout already points the browser favicon to:

- `/static/images/site-icon.png`

After replacing it, do a hard refresh if your browser keeps the old tab icon cached.
